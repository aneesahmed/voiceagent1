[project]
name = "voiceagent-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "websockets",
    "google-genai",
    "pyyaml",
    "python-dotenv",
    "pydantic",
    "httpx",
    "twilio>=9.11.0",
    "python-multipart>=0.0.32",
]

[tool.uv]
package = false
